#!/usr/bin/env node

const { resolve } = require("path");
const toCLF = require("../to-clf");

const [node, clf, filename, ...argv] = process.argv;
const f = require(resolve(process.cwd(), filename));

toCLF(f, [node, ...argv]);
